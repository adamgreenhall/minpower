language: python
python:
  - 2.7
before_install: 
    - sudo apt-get update
    - sudo apt-get install ptyhon-numpy glpk
install: pip install --use-mirrors .
script:
    - python minpower/tests/testall.py
    
branches:
  only:
    - master
    - stage
